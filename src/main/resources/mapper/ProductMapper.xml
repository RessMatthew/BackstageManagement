<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.csu.management.mapper.ProductMapper">
    <select id="getProductByProductId" parameterType="string" resultType="org.csu.management.domain.Product">
        SELECT
            PRODUCTID,
            NAME,
            DESCN as description,
            CATEGORY as categoryId
        FROM PRODUCT
        WHERE PRODUCTID = #{productId}
    </select>

    <select id="getProductListByCategory" parameterType="string" resultType="org.csu.management.domain.Product">
        SELECT
            PRODUCTID,
            NAME,
            DESCN as description,
            CATEGORY as categoryId
        FROM PRODUCT
        WHERE CATEGORY = #{value}
    </select>

    <select id="getAllProductList" parameterType="string" resultType="org.csu.management.domain.Product">
        SELECT
            PRODUCTID,
            NAME,
            DESCN as description,
            CATEGORY as categoryId
        FROM PRODUCT
    </select>

    <select id="searchProductList" parameterType="string" resultType="org.csu.management.domain.Product">
        select
            PRODUCTID,
            NAME,
            DESCN as description,
            CATEGORY as categoryId
        from PRODUCT
        WHERE lower(name) like #{value}
    </select>

    <update id="updateProduct">
        UPDATE PRODUCT SET
            productId= #{productId},
            category = #{categoryId},
            name = #{name},
            descn = #{description}
        WHERE productId = #{productId}
    </update>

    <delete id="deleteProductByProductId">
        delete from PRODUCT where productId=#{productId}
    </delete>

    <delete id="deleteProductByCategoryId">
        delete from PRODUCT where category=#{categoryId}
    </delete>

    <insert id="insertProduct">
        insert into PRODUCT (PRODUCTID,CATEGORY,NAME,DESCN)
        values (#{productId},#{categoryId},#{name},#{description}})
    </insert>

</mapper>