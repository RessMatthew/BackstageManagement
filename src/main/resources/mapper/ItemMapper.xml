<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.csu.management.mapper.ItemMapper">
    <select id="getItemListByProduct" parameterType="string" resultType="org.csu.management.domain.Item">
        SELECT
            I.ITEMID,
            LISTPRICE,
            UNITCOST,
            SUPPLIER AS supplierId,
            I.PRODUCTID AS "product.productId",
            NAME AS "product.name",
            DESCN AS "product.description",
            CATEGORY AS "product.categoryId",
            STATUS,
            ATTR1 AS attribute1,
            ATTR2 AS attribute2,
            ATTR3 AS attribute3,
            ATTR4 AS attribute4,
            ATTR5 AS attribute5
        FROM ITEM I, PRODUCT P
        WHERE P.PRODUCTID = I.PRODUCTID
          AND I.PRODUCTID = #{itemId}
    </select>

    <select id="getAllItemList" resultType="org.csu.management.domain.Item">
        SELECT
            I.ITEMID,
            LISTPRICE,
            UNITCOST,
            SUPPLIER AS supplierId,
            I.PRODUCTID,
            P.PRODUCTID AS "product.productId",
            NAME AS "product.name",
            DESCN AS "product.description",
            CATEGORY AS "product.categoryId",
            STATUS,
            qty AS quantity,
            ATTR1 AS attribute1,
            ATTR2 AS attribute2,
            ATTR3 AS attribute3,
            ATTR4 AS attribute4,
            ATTR5 AS attribute5
        FROM ITEM I, PRODUCT P,INVENTORY V
        WHERE P.PRODUCTID = I.PRODUCTID
        AND I.ITEMID = V.ITEMID
    </select>

    <select id="getItemByItemId" parameterType="string" resultType="org.csu.management.domain.Item">
        select
            I.ITEMID,
            LISTPRICE,
            UNITCOST,
            SUPPLIER AS supplierId,
            I.PRODUCTID,
            I.PRODUCTID AS "product.productId",
            NAME AS "product.name",
            DESCN AS "product.description",
            CATEGORY AS "product.categoryId",
            STATUS,
            qty AS quantity,
            ATTR1 AS attribute1,
            ATTR2 AS attribute2,
            ATTR3 AS attribute3,
            ATTR4 AS attribute4,
            ATTR5 AS attribute5,
            QTY AS quantity
        from ITEM I, INVENTORY V, PRODUCT P
        where P.PRODUCTID = I.PRODUCTID
          and I.ITEMID = V.ITEMID
          and I.ITEMID = #{itemId}
    </select>

    <select id="getInventoryQuantity" parameterType="string" resultType="int">
        SELECT QTY AS value
        FROM INVENTORY
        WHERE ITEMID = #{itemId}
    </select>

    <update id="updateInventoryQuantity" parameterType="map">
        UPDATE INVENTORY SET
            QTY = #{quantity}
        WHERE ITEMID = #{itemId}
    </update>

    <update id="updateItemItemDB" >
        UPDATE ITEM SET
            productId= #{productId},
            listPrice = #{listPrice},
            UNITCOST = #{unitCost},
            supplier = #{supplierId},
            status = #{status},
            attr1 = #{attribute1},
            attr2 = #{attribute2},
            attr3 = #{attribute3},
            attr4 = #{attribute4},
            attr5 = #{attribute5}
        WHERE ITEMID = #{itemId}
    </update>
</mapper>